
      
     
<% @items.each do |item| %>
<% product = Product.find_by(id: item.product_id)%>
<tr>
    <td>
      <%# <img src="<%= product.image%> 
      <%= image_tag product.image.tiny%>
    </td>
    <td>
      <h4><%= product.name %></h4>
      <br>
      <p><%= product.description %></p>
    </td>
    <td>
      <%= product.price %>
    </td>
    <td>
      <%= item.quantity%>
    </td>
    <td>
      $<%= item.total_price_cents.to_f / 100%></li>
    </td>
  </tr>

<% end %>